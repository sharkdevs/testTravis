language: python

python:
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install coveralls
  - pip install pytest

script:
  coverage run -m pytest -v tests/test_hello.py

after_success:
- coveralls